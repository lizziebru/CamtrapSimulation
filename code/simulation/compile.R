### compile data on multiple runs of the same simulation ###

setwd("~/Documents/Project/CamtrapSimulation/code/simulation")

setwd("results1/0.05_5e5_0_0,40/1_0.5_0.5") # set which one to analyse

# load in data:
load("seq_dats.RData")

## to do: do this once have more runs finished tomorrow